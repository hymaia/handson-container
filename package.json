{
  "name": "handson-container",
  "private": true,
  "scripts": {
    "test": "npm --prefix backend ci && npm --prefix backend test",
    "test:backend": "npm --prefix backend ci && npm --prefix backend test"
  }
}
